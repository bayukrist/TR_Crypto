<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baykris.tr_crypto">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_icx"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_icx"
        android:supportsRtl="true"
        android:theme="@style/Base.AppTheme">
        <activity android:name=".OmgActivity"></activity>
        <activity android:name=".HusdActivity" />
        <activity android:name=".EthActivity" />
        <activity android:name=".BtcActivity" />
        <activity android:name=".BnbActivity" />
        <activity android:name=".ShopList" />
        <activity android:name=".DepositActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>